node_modules/
dist/
# Capacitor native projects: keep the customized source in the repo (minSdk, permissions,
# FLAG_SECURE, network security config, notifications), but ignore build outputs & machine files.
/android/.gradle/
/android/.idea/
/android/build/
/android/app/build/
/android/app/release/
/android/capacitor-cordova-android-plugins/
/android/local.properties
/android/*.iml
/android/app/src/main/assets/public/
# iOS project not built in CI yet — keep it out of the repo for now.
/ios/
.env
.env.*
*.log
.DS_Store

# Build outputs & local helper artifacts
*.apk
ai_build.*
ai_*.ps1
ai_screen.png
ai_logcat.txt
*.marker

# icon preview artifacts (source of truth is assets/ + resources/ios)
keryx_app_icon.png
icon_*.png
shot_*.png
keryx-wallet-demo.apk
official_icon.png
official_logo.png
